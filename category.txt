<div className="flex flex-col">
            <label className="text-xs font-bold text-gray-500 mb-1 uppercase">Department *</label>
            <select required className="border-2 p-2 rounded focus:border-blue-500 outline-none" value={complaintData.assetId} onChange={(e) => setComplaintData({...complaintData, assetId: e.target.value, category: ""})}>
              <option value="">-- Select --</option>
              {assets.map((a) => <option key={a._id} value={a._id}>{a.name}</option>)}
            </select>
          </div>

          <div className="flex flex-col">
            <label className="text-xs font-bold text-gray-500 mb-1 uppercase">Category *</label>
            <select required className="border-2 p-2 rounded focus:border-blue-500 outline-none" value={complaintData.category} onChange={(e) => setComplaintData({...complaintData, category: e.target.value})} disabled={!complaintData.assetId}>
              <option value="">-- Select --</option>
              {renderCategoryOptions()}
            </select>
          </div>